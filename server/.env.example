# Copie ce fichier en ".env" (même dossier) et ajuste les valeurs selon
# ton installation MySQL locale (WampServer, XAMPP, Docker...).
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sante_vaisseau

# Port sur lequel l'API elle-même écoute (le frontend s'y connecte)
PORT=4000
